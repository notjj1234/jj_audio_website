# Desktop-only runtime pins (Windows/macOS .exe/.pkg build and local dev).
# Deliberately NOT the full project runtime: hosted backend deps (SQLAlchemy,
# redis, arq, boto3, slowapi, python-jose, passlib, ...) are excluded because
# the desktop freeze must not ship backend/. See pyproject.toml
# [project.dependencies] for the canonical runtime list, and the packages
# managed by the dependencies above.
basic-pitch[onnx]>=0.3.0
setuptools>=68,<81
onnxruntime>=1.16
pretty_midi>=0.2.10
numpy>=1.24
soundfile>=0.12
reportlab>=4.0
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.0
pydantic-settings>=2.0
yt-dlp>=2026.08
streamlit>=1.55
streamlit-local-storage>=0.0.25
mir_eval>=0.7
scipy>=1.10
httpx>=0.27
pytest>=7.0
