# Issues Audit — 2026-09-03

Static audit of the app, generated 2026-09-03. **Scope: desktop app first, website parked.** This is a
fresh audit intended to supplement the existing trackers (`issues.md`, `issues-audit-2026-09-02.md`).
New IDs start at **I-400** to avoid collisions with prior IDs (which reach I-367). Every row was
verified against the current working tree at audit time; rows where the original claim could not be
verified were dropped rather than kept.

## Legend

| Severity | Meaning |
|---|---|
| **Critical** | Don't ship a build with these: data corruption, unbounded temp-garbage, app that looks hung, OOM crashes, or dependency resolution that breaks fresh builds. |
| **High** | Real bugs users hit often: silent failures, misleading output, missing error handling, leaks that grow unbounded, memory-spike crashes on large files. |
| **Medium** | Robustness gaps, edge cases, confusing UX, or performance concerns. |
| **Low** | Polish, accessibility, cosmetic, or documentation. |
| **Parked** | Website-only (backend/ + web/ + Docker/Caddy). Nothing here affects the desktop app; revisit if the site comes back. |

**Scope tags:** `Engine` (src/audio_to_tab, shared core) · `Desktop UI` (ui/, mixer) ·
`Packaging` (installers/ffmpeg) · `CI` · `Deps` · `Tests` · `Docs` · `Web` (web/src/) ·
`API` (backend/).

## Summary

| Tier | Count |
|---|---|
| Critical | 1 |
| High | 5 |
| Medium | 15 |
| Low | 6 |
| **Active (desktop) total** | **19** |
| Parked (website, on hold) | 8 |
| **Grand total** | **27** |

---

# Tier 1 — Critical (block a desktop release)

| ID | Sev | Scope | Where | Issue | Suggested fix | Status |
|---|---|---|---|---|---|---|
| I-400 | Critical | Deps | `pyproject.toml:14,17` | Two `numpy` entries conflict: line 14 is `numpy>=1.24,<2`, line 17 is `numpy>=1.24` (no upper bound). PEP 508/pip semantics make the last entry win, so the `<2` cap is **dead** — NumPy 2.x installs and can break torch/demucs/roformer at runtime. | Delete the duplicate line 17; keep a single numpy pin with the `<2` cap used everywhere (`pyproject.toml`, `requirements.txt`). | - [ ] |

---

# Tier 2 — High

| ID | Sev | Scope | Where | Issue | Suggested fix | Status |
|---|---|---|---|---|---|---|
| I-401 | High | Engine | `scnet.py:212` | `_demix_scnet` materializes `result = torch.zeros((num_instruments, *mix.shape), float32)` for the **entire track** alongside the full `mix` tensor and activations (~8× the audio size in memory) with no chunking or size guard. A long clip OOMs the worker. | Stream overlap-add into a file or cap length via chunked processing instead of a full-track buffer. | - [ ] |
| I-402 | High | Engine | `roformer.py:438` | `_demix_track` allocates `result = np.zeros((num_stems, channels, total), float32)` plus `counter`, then loads the whole file via `_load_audio_stereo` (line 586) — the track is held several times in RAM with no size guard. OOM risk on long/large files. | Process in bounded chunks and free the source after chunking, or reject files above a size threshold. | - [ ] |
| I-403 | High | Engine | `ingest.py:457`; `pipeline.py:109` | `normalize_audio` and `_trim_audio` call `subprocess.run(...)` with **no `timeout`** (the isolate trim paths cap with `timeout_sec`). A hung ffmpeg blocks the whole isolate job or audio→PDF pipeline forever; the UI "Stop" can't interrupt it. | Add `timeout=` to both calls with `subprocess.TimeoutExpired` cleanup (kill temp output). | - [ ] |
| I-404 | High | Engine | `separate.py:280-291` | In the PyInstaller-frozen branch, `demucs_main()` runs **in-process** and ignores both `should_abort` and `timeout_sec`. A frozen-desktop user whose Demucs hangs (or hits Stop) gets no kill — and the desktop product always uses this path. | Run frozen Demucs in a child process so abort/timeout apply, or surface the limitation as a hard constraint. | - [ ] |
| I-405 | High | API | `main.py:155-159,284` | `_rate_key` reads `request.state.user_id`, but that attribute is set inside the handler body (`request.state.user_id = user.id`, line 284) — **after** the slowapi limiter's `key_func` runs. Per-user rate limiting always falls back to IP; an attacker with many IPs bypasses limits. | Move `request.state.user_id` into a dependency resolved before the limiter, or decode the bearer token in `_rate_key`. | - [ ] |

---

# Tier 3 — Medium

| ID | Sev | Scope | Where | Issue | Suggested fix | Status |
|---|---|---|---|---|---|---|
| I-406 | Medium | Engine | `separate.py:428-444` | When `output_path is None`, `temporary_output_path()` pre-creates a file on disk; if separation then raises (missing model/backend), the caller-owned temp file is never removed (no `finally`/cleanup) → orphaned WAVs accumulate. | Wrap the body so the temp file is unlinked on exception. | - [ ] |
| I-407 | Medium | Engine | `roformer.py:333-336,538-541` | `torch.load(..., weights_only=True)` falls back to `weights_only=False` on any failure. `weights_only=False` permits arbitrary pickle code execution from a checkpoint; the weights are SHA256-pinned but the fallback turns a bad/corrupt checkpoint into an execution vector. | Drop the `weights_only=False` fallback (weights are pinned — fail loudly instead) or verify hash before loading. | - [ ] |
| I-408 | Medium | Engine | `isolate.py:926-936` | `_stem_rms` calls `sf.read(always_2d=True)` per stem inside `detect_present_stems`, fully materializing every stem just to compute RMS — an unbounded memory load at the end of every 6-stem job. | Use `sf.blocks`/frame-chunked RMS or `sf.info`-based downsampled reads. | - [ ] |
| I-409 | Medium | Engine | `isolate.py:1501-1507` | The bleed gate recomputes a fresh full-track `float64` STFT **per channel** (2×) on top of earlier STFTs; for a long clip this is a large, avoidable float64 memory/time spike. | Reuse existing STFT frames or compute in float32/chunked. | - [ ] |
| I-410 | Medium | Engine | `ingest.py:378-381` | `download_youtube_audio`'s player-client retry globs-and-unlinks **every** `*.wav`/`*.part` in `out_dir`. If two downloads share an output dir (or a prior partial lives there), unrelated/concurrent output is deleted. | Scope cleanup to a per-download temp subdir or the specific failed artifacts. | - [ ] |
| I-411 | Medium | Engine | `tab_generate.py:106-157` | `_pick_chord_positions`'s recursive `search` enumerates all candidate combinations of positions for a chord's pitches with no memoization/cap; dense 5–6-note chords can blow up. | Cap candidates per pitch or use greedy/beam assignment. | - [ ] |
| I-412 | Medium | Desktop UI | `isolate.py:358-363,762` | `_stem_waveform_peaks` reads the full WAV (`waveform_peaks` → `sf.read`) on **every** `_render_live_mixer` run (per-stem, called at line 762). Streamlit reruns on any interaction, so peaks recompute constantly. | Cache peaks keyed on `(path, mtime, num_points)` via `functools.lru_cache` or a dict memo. | - [ ] |
| I-413 | Medium | Desktop UI | `frontend/src/main.ts:660` (via `onTimeUpdate` 277) | `saveTransport()` writes JSON to `sessionStorage` on **every** animation frame (~60×/sec) during playback → GC pressure / jank on low-end devices. | Throttle `saveTransport()` to ≤1/sec. | - [ ] |
| I-414 | Medium | Desktop UI | `frontend/src/main.ts:449-482` | `renderUI` replaces the entire DOM via `innerHTML` (lines 458, 482), tearing down the tree so a mid-drag interaction is cancelled and the pointer is orphaned on a parent rerun. | Use targeted DOM updates (patch only changed stems/controls) or gate on `stemKey` changes. | - [ ] |
| I-415 | Medium | Desktop UI | `frontend/src/main.ts:170-182` | `loadStems` calls `applyGains(...)` defaulting all stems to 0 dB **before** `applyStateGains()` (line 791) applies the saved state — a brief all-0-dB volume pop on slow loads. | Don't apply gains in `loadStems`; let `onRender` handle it solely via `applyStateGains`. | - [ ] |
| I-416 | Medium | Deps | `pyproject.toml:7`; `src/audio_to_tab/__init__.py:3`; `packaging/make_app.sh:42`; `packaging/make_pkg.sh:15`; `packaging/make_dmg.sh:14`; `packaging/AudioTools.iss:19` | Version `0.1.3` is hardcoded in 6 places. A bump that misses any one ships mismatched installer/app metadata that rots silently. | Derive the version once (e.g. from `pyproject.toml` via `importlib.metadata` / a `tomllib` read) and feed it to scripts and the `.iss` `#define`. | - [ ] |
| I-417 | Medium | Deps | `requirements.txt:19` | `yt-dlp>=2026.08` is a **future-denominated** floor (an exact month, not a semver release) with no upper bound — any breaking yt-dlp change silently breaks the desktop build. | Pin to an actual release tag (e.g. `yt-dlp==2026.8.28`) or add an upper bound. | - [ ] |
| I-418 | Medium | Deps | `requirements.txt:20-21,13-14,15-17` | Dev/eval/test-only deps (`pytest`, `httpx`, `mir_eval`, `scipy`) are bundled into the desktop runtime freeze file, bloating the installer and broadening its attack surface. | Move them to a `[dev]`/`[eval]` extra; keep the desktop `requirements.txt` runtime-only. | - [ ] |
| I-419 | Medium | Deps | `pyproject.toml:18` vs `requirements.txt` | `librosa>=0.10` is a core dep in `pyproject.toml` but **absent** from the desktop freeze `requirements.txt` — a frozen app that imports `librosa` fails with `ModuleNotFoundError`. | Add `librosa` to `requirements.txt` or confirm no frozen codepath imports it and move it to an optional extra. | - [ ] |

---

# Tier 4 — Low

| ID | Sev | Scope | Where | Issue | Suggested fix | Status |
|---|---|---|---|---|---|---|
| I-420 | Low | Desktop UI | `isolate.py:1883,1900` | Duplicate widget key `"isolate_save_tracks"` across the `if ready` / `else` branches. Safe today (one branch renders per run) but fragile if Streamlit changes widget-identity tracking. | Use distinct keys per branch. | - [ ] |
| I-421 | Low | Desktop UI | `frontend/src/main.ts:274` | `tick` calls `void this.stopSources(false)` (fire-and-forget) inside `requestAnimationFrame`; `stopSources` is currently synchronous but is declared `async`, so a future `await` inside it would silently change timing. | Remove the `async` or await it within an async `tick`. | - [ ] |
| I-422 | Low | Desktop UI | `frontend/src/main.ts:296-302` | `transportBusy` has no timeout — if `engine.play()/stop()` hangs (e.g. blocked AudioContext), transport buttons stay disabled with no recovery path. | Add a short timeout that resets `transportBusy` and reports an error. | - [ ] |
| I-423 | Low | Desktop UI | `isolate.py:1030-1053` | `_render_section_preview` fails silently: if both the preview build and the `st.audio` fallback fail, the section just disappears with no user feedback. | Add a `st.caption("Preview unavailable")`/warning in the innermost `except`. | - [ ] |
| I-424 | Low | Engine | `subprocess_util.py:72` | `_run_process` polls `communicate(timeout=0.5)` ~2×/sec for the whole job even when idle/no abort — avoidable wakeup overhead on a many-minute Demucs run. | Back off the poll interval once `should_abort`/`timeout_sec` are known None. | - [ ] |
| I-425 | Low | Docs | `desktop-release.yml:2-3,220` | Not a bug (the workflow **explicitly** documents that CI builds are unsigned), but worth a tracker note: every CI `.pkg`/`.exe` is unsigned and triggers Gatekeeper/SmartScreen warnings — a known pain point for testers. | If signing is ever wanted, wire cert import + notary secrets; otherwise keep the docs note and point testers to DESKTOP.md. | - [ ] |

---

# Parked — Website (backend/ + web/ + Docker/Caddy, on hold)

| ID | Sev | Scope | Where | Issue | Suggested fix | Status |
|---|---|---|---|---|---|---|
| I-450 | High | API | `events.py:38-47` | `publish_redis` constructs a brand-new `redis.from_url(...)` client on **every** call and never closes it. It's called per progress stage per job → the Redis connection pool/file descriptors grow unbounded over time. | Cache a single client/pool on the `EventBus` and close on shutdown. | - [ ] |
| I-451 | High | Docker | `docker-compose.yml:30,79-81` | Postgres uses hardcoded `att:att` credentials with no env-var/secrets override, and Redis (`ATT_REDIS_URL`, `redis://redis:6379/0`) runs with **no password**. Any compromised container on the network gets unrestricted DB/Redis access. | Use `${POSTGRES_PASSWORD:?}`-style env/secrets and add Redis `requirepass`. | - [ ] |
| I-452 | Medium | Docker | `docker-compose.yml` (compose) | No `deploy.resources.limits`/`mem_limit` on any service. A Demucs job can OOM-kill Postgres/Redis or the host on a memory-constrained server. | Add CPU/memory limits (worker generous, API/DB/Redis bounded). | - [ ] |
| I-453 | Medium | Docker | `docker-compose.yml:90-91,47-49,66-68` | Redis has **no healthcheck**; api/worker use `condition: service_started` (process-launched, not ready) so startup can race Redis readiness. | Add `redis-cli ping` healthcheck and `service_healthy`. | - [ ] |
| I-454 | Medium | Web | `web/src/api.ts:171,193` | `watchJob` opens a WebSocket **and** starts an unconditional 2s HTTP poll (line 193); both call `onUpdate` independently, so every job deliverable doubles API traffic and re-fires UI updates. | Start the poll only on WS close/error, and cancel it once the WS delivers. | - [ ] |
| I-455 | Medium | API | `backend/main.py:507-518,542-553` | S3 artifact download uses `open_temp()` + cleanup `tmp.unlink` inside a generator passed to `StreamingResponse`; if the client disconnects mid-transfer the `finally` may not run → orphan temp files grow on disk. | Move cleanup to `BackgroundTasks`/`asyncio.shield` on the response. | - [ ] |
| I-456 | Low | API | `backend/main.py:226,259` | Refresh cookie is set with `samesite="lax"`; `strict` is safer for a refresh token (this SPA has no cross-site use case). | Change to `samesite="strict"`. | - [ ] |
| I-457 | Low | Docker | `docker-compose.yml:96` | `minio/minio:latest` (and `minio/mc:latest`) pull whatever is newest on rebuild — a breaking MinIO change silently breaks the S3 backend. | Pin to a specific MinIO release tag. | - [ ] |

---

### Notes on dropped candidates

The working-tree audit surfaced a few items that were **verified as non-issues** and deliberately excluded:

- **Desktop release "unsigned"** — `desktop-release.yml:2-3` explicitly documents that CI builds are unsigned; this is a known state, not a hidden misrepresentation (kept only as Low note I-425).
- **macOS CI "skips all tests"** — `ci.yml:54-57` runs tests on macOS (hosted-backend job deselected). Not a gap.
- **`setuptools` "unbounded"** — `requirements.txt:12` is `setuptools>=68,<81`; already capped. Not a gap.
- **Silent config overrides in `IsolateConfig`** — the claimed `isolate.py:884-891` range pointed at an unrelated function; could not be confirmed. Dropped rather than risk a stale row.
